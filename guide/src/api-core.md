# Core API Reference

Documentation for core Mojentic types and traits.

## Coming Soon

This guide will provide detailed API documentation for:
- `Broker` - The main entry point
- `Message` - Message types and builders
- `CompletionConfig` - Configuration options
- `ToolCall` and `ToolResult` - Tool interaction types
- Core traits and their implementations

For now, use `cargo doc --open` to browse the full API documentation.

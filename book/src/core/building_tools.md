# Building Tools

Expose Rust functions as tools the LLM can call.

- Define tool signature and description
- Register with the broker
- Handle tool calls in a loop

(Placeholder: concrete trait/impl snippets once tool API stabilized.)

# Structured Output

Generate structured JSON-like responses parsed directly into Rust types.

Design goal: eliminate brittle regex/post-hoc parsing.

(Placeholder: Example using serde + output schema once implemented.)
